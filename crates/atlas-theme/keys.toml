# The theme-key registry — the one hand-edited source for every Atlas theme key.
#
# GENERATED FROM THIS FILE (never edit those by hand; run `bun run theme:keys`):
#
#   src/features/theme/theme-key-registry.ts   the TS registry the resolver reads
#   crates/atlas-theme/theme-keys.txt          the key list + descriptions Rust reads
#   crates/atlas-theme/schema/theme-v1.json    the `keys` property, so `#:schema`
#                                              gives authors completion and flags a typo
#   docs/reference/theme-keys.md               the generated part of the reference
#
# `bun run theme:keys:check` (and `bun run test`) fail when any of them is stale.
#
# ── An entry ──────────────────────────────────────────────────────────────────
#
#   name         the dotted role name an author writes under `[dark.keys]`.
#   group        heading this key sits under in the generated outputs. Keys of a
#                group must be contiguous.
#   description  one line, what this key colours. Shown on hover in a TOML editor.
#   palette      first derivation source: one of the eight palette hues.
#   base         second derivation source: a shadcn base token.
#   op/amount    the transform applied to whichever source was chosen. Omit for a
#                plain alias.
#   dark/light   the Atlas default for each appearance, used when a theme sets
#                neither the key nor any source it derives from.
#   note         optional prose, carried into the generated TS as a comment.
#
# Resolution order, implemented once in `resolve-theme.ts`: an explicit `keys`
# value in the theme wins; then `palette`; then `base`; then the appearance
# default. `op` is applied to whichever of those three was used — never to an
# explicit value, which is taken exactly as written.
#
# ── The operation vocabulary (closed; adding one means editing the resolver) ──
#
#   alpha              replace the colour's alpha channel with `amount`.
#   lighten            mix `amount` of white into the colour.
#   toward_background  mix `amount` of `base.background` into the colour. Keeps a
#                      muted role muted on a light theme, where lightening would
#                      wash it out.
#
# Deleting a key, renaming one, or changing what it derives from is an edit to
# this file and nothing else.
#
# ── `[[derived]]`: a colour Atlas writes but no theme may set ─────────────────
#
# Some `--atlas-…` variables the app reads are not judgement calls. A status
# BADGE fill is its status FOREGROUND at 12% — an author who wants a different
# green wants a different green, not two greens that must be kept in step. So
# those live here instead of as keys: Atlas resolves them to exactly the value
# a key would have produced, and they are absent from `theme-keys.txt` and from
# the JSON Schema, so writing one in a theme file is an unknown-key warning.
#
#   name/description  as for a key.
#   from              the settable key this transforms, OR
#   base              the shadcn base token it transforms. Exactly one of the two.
#   op/amount         required — a derived variable with no transform would be
#                     an alias, and an alias should just be the key itself.
#
# Reach for this when a colour is a FUNCTION of a key. Anything a theme author
# would genuinely want to set independently is a key, however small.

version = 1


# The groups, in order. Every key names one, and a group's keys are contiguous.
[[group]]
name = "Borders"
description = "Separators and control outlines."

[[group]]
name = "Elements and overlays"
description = "Hover/selected/pressed overlays, the raised edge, and the brand fills."

[[group]]
name = "Text"
description = "Prose roles that are not a base token."

[[group]]
name = "Status"
description = "Success / warning / error / info."

[[group]]
name = "Selection"
description = "Document text selection."

[[group]]
name = "Terminal"
description = "The PTY surface and the 16 ANSI colours."

[[group]]
name = "Syntax"
description = "CodeMirror and Markdown highlighting."

[[group]]
name = "Editor"
description = "The code editor's own chrome."

[[group]]
name = "Chrome surfaces"
description = "Scrollbars and panel surfaces — the app frame."

[[group]]
name = "Diff"
description = "Added and removed regions, inline and side-by-side."

[[group]]
name = "Agents and indicators"
description = "The agent identity chip."


# ── Borders ───────────────────────────────────────────────────────────────────

[[key]]
name = "border.subtle"
group = "Borders"
description = "Low-emphasis separator."
base = "sidebar-border"
dark = "#141414"
light = "#ebe5de"

[[key]]
name = "border.strong"
group = "Borders"
description = "High-emphasis border, including a focused control's."
base = "border"
dark = "#3d3d3d"
light = "#b8b1aa"
note = """
Sourced from `border`, not `ring`. It used to be `ring`, and decision 31 then
made `ring` a real accent — so a theme that set `ring` and left this alone got
a brand-coloured hairline everywhere it wanted a strong grey one. The two roles
are genuinely different: `ring` is the focus RING, this is the strongest
BORDER, and a focused control's border is this rather than a third key.
"""


# ── Elements and overlays ─────────────────────────────────────────────────────

[[key]]
name = "element.hover"
group = "Elements and overlays"
description = "Hover overlay for ordinary elements."
base = "foreground"
op = "alpha"
amount = 0.04
dark = "rgba(255,255,255,0.04)"
light = "rgba(0,0,0,0.04)"

[[key]]
name = "element.selected"
group = "Elements and overlays"
description = "Selected overlay for ordinary elements."
base = "foreground"
op = "alpha"
amount = 0.06
dark = "rgba(255,255,255,0.06)"
light = "rgba(0,0,0,0.06)"

[[key]]
name = "element.active"
group = "Elements and overlays"
description = "Pressed overlay for ordinary elements."
base = "foreground"
op = "alpha"
amount = 0.08
dark = "rgba(255,255,255,0.08)"
light = "rgba(0,0,0,0.08)"

[[key]]
name = "element.highlight"
group = "Elements and overlays"
description = "Top-edge highlight on a raised surface."
base = "foreground"
op = "alpha"
amount = 0.06
dark = "rgba(255,255,255,0.06)"
light = "rgba(0,0,0,0.06)"
note = """
The 1px top edge on raised glass — `inset-highlight` in globals.css. It is
the theme's own foreground at 6%, so a light variant gets a dark edge
instead of the white-on-white one a hardcoded highlight would give it.
"""

[[key]]
name = "primary.hover"
group = "Elements and overlays"
description = "Hovered primary-brand fill."
base = "primary"
op = "lighten"
amount = 0.15
dark = "#cccccc"
light = "#a290b5"

[[key]]
name = "primary.muted"
group = "Elements and overlays"
description = "Muted primary-brand fill."
base = "primary"
op = "alpha"
amount = 0.06
dark = "rgba(255,255,255,0.06)"
light = "rgba(144,122,169,0.08)"


# ── Text ──────────────────────────────────────────────────────────────────────

[[key]]
name = "text.disabled"
group = "Text"
description = "Disabled and unavailable text."
base = "muted-foreground"
op = "toward_background"
amount = 0.35
dark = "#333333"
light = "#b8b1aa"


# ── Status ────────────────────────────────────────────────────────────────────

[[key]]
name = "status.success.foreground"
group = "Status"
description = "Success status foreground, and the live-capture indicator."
palette = "green"
dark = "#3fb950"
light = "#286983"

[[key]]
name = "status.warning.foreground"
group = "Status"
description = "Warning status foreground."
palette = "yellow"
dark = "#cd9731"
light = "#ea9d34"

[[key]]
name = "status.error.foreground"
group = "Status"
description = "Error status foreground."
palette = "red"
base = "destructive"
dark = "#f44747"
light = "#b4637a"

[[key]]
name = "status.info.foreground"
group = "Status"
description = "Informational status foreground."
palette = "blue"
dark = "#6796e6"
light = "#56949f"


# ── Selection ─────────────────────────────────────────────────────────────────

[[key]]
name = "selection.background"
group = "Selection"
description = "Document text selection."
base = "primary"
op = "alpha"
amount = 0.18
dark = "rgba(255,255,255,0.18)"
light = "rgba(144,122,169,0.18)"


# ── Terminal ──────────────────────────────────────────────────────────────────

[[key]]
name = "terminal.foreground"
group = "Terminal"
description = "Terminal default foreground."
base = "foreground"
dark = "#d4d4d4"
light = "#575279"

[[key]]
name = "terminal.background"
group = "Terminal"
description = "Terminal background."
base = "background"
dark = "#000000"
light = "#faf4ed"

[[key]]
name = "terminal.cursor"
group = "Terminal"
description = "Terminal cursor."
base = "foreground"
dark = "#d4d4d4"
light = "#575279"

[[key]]
name = "terminal.ansi.black"
group = "Terminal"
description = "ANSI black."
base = "background"
op = "lighten"
amount = 0.12
dark = "#1e1e1e"
light = "#575279"

[[key]]
name = "terminal.ansi.red"
group = "Terminal"
description = "ANSI red."
palette = "red"
dark = "#f44747"
light = "#b4637a"

[[key]]
name = "terminal.ansi.green"
group = "Terminal"
description = "ANSI green."
palette = "green"
dark = "#98c379"
light = "#286983"

[[key]]
name = "terminal.ansi.yellow"
group = "Terminal"
description = "ANSI yellow."
palette = "yellow"
dark = "#e5c07b"
light = "#ea9d34"

[[key]]
name = "terminal.ansi.blue"
group = "Terminal"
description = "ANSI blue."
palette = "blue"
dark = "#61afef"
light = "#56949f"

[[key]]
name = "terminal.ansi.magenta"
group = "Terminal"
description = "ANSI magenta."
palette = "purple"
dark = "#c678dd"
light = "#907aa9"

[[key]]
name = "terminal.ansi.cyan"
group = "Terminal"
description = "ANSI cyan."
palette = "cyan"
dark = "#56b6c2"
light = "#d7827e"

[[key]]
name = "terminal.ansi.white"
group = "Terminal"
description = "ANSI white."
base = "foreground"
dark = "#d4d4d4"
light = "#575279"

[[key]]
name = "terminal.ansi.bright_black"
group = "Terminal"
description = "ANSI bright black."
base = "muted-foreground"
dark = "#666666"
light = "#9893a5"

[[key]]
name = "terminal.ansi.bright_red"
group = "Terminal"
description = "ANSI bright red."
palette = "red"
op = "lighten"
amount = 0.15
dark = "#ff6b6b"
light = "#c97991"

[[key]]
name = "terminal.ansi.bright_green"
group = "Terminal"
description = "ANSI bright green."
palette = "green"
op = "lighten"
amount = 0.15
dark = "#b2d89a"
light = "#4d8399"

[[key]]
name = "terminal.ansi.bright_yellow"
group = "Terminal"
description = "ANSI bright yellow."
palette = "yellow"
op = "lighten"
amount = 0.15
dark = "#f2d28c"
light = "#edae52"

[[key]]
name = "terminal.ansi.bright_blue"
group = "Terminal"
description = "ANSI bright blue."
palette = "blue"
op = "lighten"
amount = 0.15
dark = "#82c0f3"
light = "#73a5ae"

[[key]]
name = "terminal.ansi.bright_magenta"
group = "Terminal"
description = "ANSI bright magenta."
palette = "purple"
op = "lighten"
amount = 0.15
dark = "#d493e5"
light = "#a290b5"

[[key]]
name = "terminal.ansi.bright_cyan"
group = "Terminal"
description = "ANSI bright cyan."
palette = "cyan"
op = "lighten"
amount = 0.15
dark = "#78c7d0"
light = "#dd9794"

[[key]]
name = "terminal.ansi.bright_white"
group = "Terminal"
description = "ANSI bright white."
base = "foreground"
op = "lighten"
amount = 0.18
dark = "#ffffff"
light = "#464261"


# ── Syntax ────────────────────────────────────────────────────────────────────

[[key]]
name = "syntax.comment"
group = "Syntax"
description = "Comments and prose quotes."
base = "muted-foreground"
dark = "#8f8f8f"
light = "#9893a5"

[[key]]
name = "syntax.keyword"
group = "Syntax"
description = "Keywords and control flow."
palette = "purple"
dark = "#c9a2f5"
light = "#286983"

[[key]]
name = "syntax.string"
group = "Syntax"
description = "Strings."
palette = "green"
dark = "#9ecf8a"
light = "#ea9d34"

[[key]]
name = "syntax.number"
group = "Syntax"
description = "Numbers."
palette = "orange"
dark = "#e0b070"
light = "#ea9d34"

[[key]]
name = "syntax.type"
group = "Syntax"
description = "Types, classes, and namespaces."
palette = "cyan"
dark = "#7fd1e8"
light = "#56949f"

[[key]]
name = "syntax.function"
group = "Syntax"
description = "Functions and headings."
palette = "blue"
base = "primary"
dark = "#ffff00"
light = "#286983"

[[key]]
name = "syntax.variable"
group = "Syntax"
description = "Variables."
base = "foreground"
dark = "#eaeaea"
light = "#575279"

[[key]]
name = "syntax.operator"
group = "Syntax"
description = "Operators."
palette = "cyan"
dark = "#9a9a9a"
light = "#797593"

[[key]]
name = "syntax.tag"
group = "Syntax"
description = "Markup tags."
palette = "red"
dark = "#7fd1e8"
light = "#b4637a"

[[key]]
name = "syntax.attribute"
group = "Syntax"
description = "Markup attributes."
palette = "yellow"
dark = "#d9b47a"
light = "#907aa9"

[[key]]
name = "syntax.constant"
group = "Syntax"
description = "Constants and atoms."
palette = "orange"
dark = "#e0b070"
light = "#d7827e"

[[key]]
name = "syntax.regexp"
group = "Syntax"
description = "Regular expressions."
palette = "red"
dark = "#e59a72"
light = "#b4637a"

[[key]]
name = "syntax.escape"
group = "Syntax"
description = "Escape sequences."
palette = "pink"
dark = "#e59a72"
light = "#d7827e"

[[key]]
name = "syntax.definition"
group = "Syntax"
description = "Definitions and strong prose."
base = "foreground"
dark = "#ffffff"
light = "#464261"

[[key]]
name = "syntax.property"
group = "Syntax"
description = "Properties and object keys."
palette = "cyan"
dark = "#c8c8c8"
light = "#56949f"


# ── Editor ────────────────────────────────────────────────────────────────────

[[key]]
name = "editor.background"
group = "Editor"
description = "Code editor background."
base = "background"
dark = "#000000"
light = "#faf4ed"

[[key]]
name = "editor.foreground"
group = "Editor"
description = "Code editor foreground."
base = "foreground"
dark = "#d4d4d4"
light = "#575279"

[[key]]
name = "editor.caret"
group = "Editor"
description = "Code editor caret."
base = "foreground"
dark = "#d4d4d4"
light = "#575279"

[[key]]
name = "editor.gutter.background"
group = "Editor"
description = "Code editor gutter background."
base = "background"
dark = "#000000"
light = "#faf4ed"

[[key]]
name = "editor.gutter.foreground"
group = "Editor"
description = "Code editor line numbers."
base = "muted-foreground"
dark = "#666666"
light = "#9893a5"

[[key]]
name = "editor.active_line.background"
group = "Editor"
description = "Active editor line."
base = "foreground"
op = "alpha"
amount = 0.04
dark = "rgba(255,255,255,0.04)"
light = "rgba(0,0,0,0.04)"

[[key]]
name = "editor.active_line.gutter_foreground"
group = "Editor"
description = "Active line number."
base = "foreground"
dark = "#d4d4d4"
light = "#575279"

[[key]]
name = "editor.selection.background"
group = "Editor"
description = "Editor selection."
base = "primary"
op = "alpha"
amount = 0.24
dark = "#303030"
light = "#dfdad9"

[[key]]
name = "editor.match_bracket.background"
group = "Editor"
description = "Matching bracket background."
base = "accent"
dark = "#2d2d2d"
light = "#dfdad9"

[[key]]
name = "editor.match_bracket.border"
group = "Editor"
description = "Matching bracket outline."
base = "ring"
dark = "#3d3d3d"
light = "#907aa9"


# ── Chrome surfaces ───────────────────────────────────────────────────────────

[[key]]
name = "scrollbar.thumb.background"
group = "Chrome surfaces"
description = "Scrollbar thumb."
base = "foreground"
op = "alpha"
amount = 0.16
dark = "rgba(255,255,255,0.16)"
light = "rgba(0,0,0,0.16)"

[[key]]
name = "scrollbar.thumb.hover"
group = "Chrome surfaces"
description = "Hovered scrollbar thumb."
base = "foreground"
op = "alpha"
amount = 0.26
dark = "rgba(255,255,255,0.26)"
light = "rgba(0,0,0,0.26)"

[[key]]
name = "panel.background"
group = "Chrome surfaces"
description = "Panel background."
base = "sidebar"
dark = "#060706"
light = "#fffaf3"

[[key]]
name = "panel.input.background"
group = "Chrome surfaces"
description = "Panel input background."
base = "background"
dark = "#0a0a0a"
light = "#fffaf3"


# ── Diff ──────────────────────────────────────────────────────────────────────

[[key]]
name = "diff.added.background"
group = "Diff"
description = "Added line or hunk."
palette = "green"
op = "alpha"
amount = 0.13
dark = "#0d2211"
light = "rgba(40,105,131,0.13)"

[[key]]
name = "diff.added.emphasis"
group = "Diff"
description = "Changed words inside an added line."
palette = "green"
op = "alpha"
amount = 0.34
dark = "rgba(52,211,153,0.34)"
light = "rgba(40,105,131,0.34)"

[[key]]
name = "diff.added.text"
group = "Diff"
description = "Added diff text, and the +N line statistic."
palette = "green"
dark = "#3fb950"
light = "#286983"

[[key]]
name = "diff.removed.background"
group = "Diff"
description = "Removed line or hunk."
palette = "red"
op = "alpha"
amount = 0.13
dark = "#220d0d"
light = "rgba(180,99,122,0.13)"

[[key]]
name = "diff.removed.emphasis"
group = "Diff"
description = "Changed words inside a removed line."
palette = "red"
op = "alpha"
amount = 0.34
dark = "rgba(244,63,63,0.34)"
light = "rgba(180,99,122,0.34)"

[[key]]
name = "diff.removed.text"
group = "Diff"
description = "Removed diff text, and the -N line statistic."
palette = "red"
dark = "#f85149"
light = "#b4637a"

[[key]]
name = "diff.context.background"
group = "Diff"
description = "Unchanged diff context."
base = "background"
dark = "#0a0a0a"
light = "#faf4ed"



# ── Agents and indicators ─────────────────────────────────────────────────────

[[key]]
name = "agent.chip.foreground"
group = "Agents and indicators"
description = "Agent identity chip text."
base = "foreground"
dark = "#ffffff"
light = "#575279"
note = """
Two keys, not eighteen. There WAS one pair per vendor — and eight of those
named agents that do not exist, while three more were overridden by hardcoded
CSS. ADR-0002 forbids privileging an agent, and the installed set is discovered
at runtime, so a theme author cannot enumerate it in the first place. The
vendors' own brand hues are now plain constants beside `agentMeta()`; this pair
is the neutral chip every identity falls back to, which is what lets a
monochrome theme flatten the lot.
"""

[[key]]
name = "agent.chip.background"
group = "Agents and indicators"
description = "Agent identity chip fill."
base = "foreground"
op = "alpha"
amount = 0.06
dark = "rgba(255,255,255,0.06)"
light = "rgba(0,0,0,0.06)"


# ── Derived variables ─────────────────────────────────────────────────────────
#
# Not theme keys. See the header for what belongs here.

[[derived]]
name = "status.success.background"
description = "Tinted fill behind a success foreground."
from = "status.success.foreground"
op = "alpha"
amount = 0.12

[[derived]]
name = "status.warning.background"
description = "Tinted fill behind a warning foreground."
from = "status.warning.foreground"
op = "alpha"
amount = 0.12

[[derived]]
name = "status.error.background"
description = "Tinted fill behind an error foreground."
from = "status.error.foreground"
op = "alpha"
amount = 0.12

[[derived]]
name = "status.info.background"
description = "Tinted fill behind an informational foreground."
from = "status.info.foreground"
op = "alpha"
amount = 0.12

[[derived]]
name = "element.emphasis"
description = "The strongest neutral overlay — a chat mention addressed to you."
base = "foreground"
op = "alpha"
amount = 0.16

[[derived]]
name = "terminal.selection"
description = "Terminal selection."
base = "primary"
op = "alpha"
amount = 0.3
