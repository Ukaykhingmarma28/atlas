import { MissionControlDashboard } from "./dashboard/mission-control-dashboard";

/** Tab `mission-control` — the cross-project analytics dashboard. */
export function MissionControlPanel() {
  return <MissionControlDashboard />;
}
