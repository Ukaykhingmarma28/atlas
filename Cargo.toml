# Atlas's root cargo workspace.
#
# The repo had no `[workspace]` until now, and the reason was real: the old ACP
# stack pinned `agent-client-protocol` 1.3 whose schema crate is pinned exactly
# (`=1.4.0`), the ported stack pins 2.0 / `=1.5.0`, and no single cargo
# resolution can hold two exact pins of the same crate. The old stack is gone
# and every remaining consumer is on `=2.0.0`, so the collision is gone with it
# (spec `docs/archive/atlas-agent-codex-port-spec.md`, decision D4 and its premise
# correction; issue #38).
#
# The workspace exists so the vendored engine (#42) lands in the same
# dependency graph as the app instead of a parallel one. Nothing about how
# Atlas builds today changes: the patch tables and profiles below are the ones
# that were in `src-tauri/Cargo.toml`, moved rather than rewritten, because
# cargo honors both ONLY in the manifest it was invoked on — which, in a
# workspace, is always this file.
#
# `tests/cargo-workspace.test.ts` enforces the invariants that have no compile
# error to announce them: patches and profiles nowhere but here, and a dev
# opt-level restated for every member (see `[profile.dev.package."*"]` below).

[workspace]
resolver = "2"

members = [
  "crates/atlas-acp-thread",
  "crates/atlas-agent-delta",
  "crates/atlas-agent-manager",
  "crates/atlas-agent-servers",
  "crates/atlas-agent-store",
  "crates/atlas-agent-transcript",
  "crates/atlas-agent-wire",
  "crates/atlas-bus",
  "crates/atlas-checkpoint",
  "crates/atlas-codeindex",
  "crates/atlas-comms",
  "crates/atlas-embed",
  "crates/atlas-git",
  "crates/atlas-gitdiff",
  "crates/atlas-icon-theme",
  "crates/atlas-memory",
  "crates/atlas-native-agent",
  "crates/atlas-process",
  "crates/atlas-redact",
  "crates/atlas-terminal",
  "crates/atlas-theme",
  "crates/atlas-thread-metadata",
  "src-tauri",

  # The vendored engine — see the block below.
  "vendor/atlas-engine/agent-graph-store",
  "vendor/atlas-engine/agent-identity",
  "vendor/atlas-engine/analytics",
  "vendor/atlas-engine/app-server",
  "vendor/atlas-engine/app-server-client",
  "vendor/atlas-engine/app-server-protocol",
  "vendor/atlas-engine/app-server-protocol-noop-macros",
  "vendor/atlas-engine/app-server-transport",
  "vendor/atlas-engine/app-server/tests/common",
  "vendor/atlas-engine/apply-patch",
  "vendor/atlas-engine/arg0",
  "vendor/atlas-engine/async-utils",
  "vendor/atlas-engine/aws-auth",
  "vendor/atlas-engine/backend-client",
  "vendor/atlas-engine/chatgpt",
  "vendor/atlas-engine/cloud-config",
  "vendor/atlas-engine/code-mode",
  "vendor/atlas-engine/code-mode-protocol",
  "vendor/atlas-engine/atlas-engine-api",
  "vendor/atlas-engine/atlas-engine-backend-openapi-models",
  "vendor/atlas-engine/atlas-engine-client",
  "vendor/atlas-engine/atlas-engine-experimental-api-macros",
  "vendor/atlas-engine/atlas-engine-home",
  "vendor/atlas-engine/atlas-engine-mcp",
  "vendor/atlas-engine/collaboration-mode-templates",
  "vendor/atlas-engine/config",
  "vendor/atlas-engine/connectors",
  "vendor/atlas-engine/context-fragments",
  "vendor/atlas-engine/core",
  "vendor/atlas-engine/core-plugins",
  "vendor/atlas-engine/core/tests/common",
  "vendor/atlas-engine/diagnostics",
  "vendor/atlas-engine/exec-server",
  "vendor/atlas-engine/exec-server-protocol",
  "vendor/atlas-engine/exec-server/tests/support",
  "vendor/atlas-engine/execpolicy",
  "vendor/atlas-engine/ext/agent",
  "vendor/atlas-engine/ext/connectors",
  "vendor/atlas-engine/ext/extension-api",
  "vendor/atlas-engine/ext/git-attribution",
  "vendor/atlas-engine/ext/goal",
  "vendor/atlas-engine/ext/guardian",
  "vendor/atlas-engine/ext/guardian-v2",
  "vendor/atlas-engine/ext/image-generation",
  "vendor/atlas-engine/ext/items",
  "vendor/atlas-engine/ext/mcp",
  "vendor/atlas-engine/ext/memories",
  "vendor/atlas-engine/ext/queue",
  "vendor/atlas-engine/ext/skills",
  "vendor/atlas-engine/ext/web-search",
  "vendor/atlas-engine/external-agent-migration",
  "vendor/atlas-engine/features",
  "vendor/atlas-engine/feedback",
  "vendor/atlas-engine/file-search",
  "vendor/atlas-engine/file-system",
  "vendor/atlas-engine/file-watcher",
  "vendor/atlas-engine/git-utils",
  "vendor/atlas-engine/history",
  "vendor/atlas-engine/hooks",
  "vendor/atlas-engine/http-client",
  "vendor/atlas-engine/install-context",
  "vendor/atlas-engine/keyring-store",
  "vendor/atlas-engine/linux-sandbox",
  "vendor/atlas-engine/login",
  "vendor/atlas-engine/memories/read",
  "vendor/atlas-engine/memories/write",
  "vendor/atlas-engine/model-provider",
  "vendor/atlas-engine/model-provider-info",
  "vendor/atlas-engine/models-manager",
  "vendor/atlas-engine/network-proxy",
  "vendor/atlas-engine/otel",
  "vendor/atlas-engine/plugin",
  "vendor/atlas-engine/process-hardening",
  "vendor/atlas-engine/prompts",
  "vendor/atlas-engine/protocol",
  "vendor/atlas-engine/response-debug-context",
  "vendor/atlas-engine/rmcp-client",
  "vendor/atlas-engine/rollout",
  "vendor/atlas-engine/rollout-trace",
  "vendor/atlas-engine/sandboxing",
  "vendor/atlas-engine/secrets",
  "vendor/atlas-engine/shell-command",
  "vendor/atlas-engine/shell-escalation",
  "vendor/atlas-engine/skills",
  "vendor/atlas-engine/state",
  "vendor/atlas-engine/terminal-detection",
  "vendor/atlas-engine/test-binary-support",
  "vendor/atlas-engine/thread-store",
  "vendor/atlas-engine/tools",
  "vendor/atlas-engine/uds",
  "vendor/atlas-engine/utils/absolute-path",
  "vendor/atlas-engine/utils/audio",
  "vendor/atlas-engine/utils/cache",
  "vendor/atlas-engine/utils/cargo-bin",
  "vendor/atlas-engine/utils/cli",
  "vendor/atlas-engine/utils/home-dir",
  "vendor/atlas-engine/utils/image",
  "vendor/atlas-engine/utils/json-to-toml",
  "vendor/atlas-engine/utils/output-truncation",
  "vendor/atlas-engine/utils/path-uri",
  "vendor/atlas-engine/utils/path-utils",
  "vendor/atlas-engine/utils/plugins",
  "vendor/atlas-engine/utils/pty",
  "vendor/atlas-engine/utils/rustls-provider",
  "vendor/atlas-engine/utils/stream-parser",
  "vendor/atlas-engine/utils/string",
  "vendor/atlas-engine/utils/template",
  "vendor/atlas-engine/websocket-client",
  "vendor/atlas-engine/windows-sandbox-rs",
  "vendor/atlas-engine/workload-identity",
]

# Deliberately outside the workspace. Each is built on its own, with its own
# lockfile and its own profile, and joining the workspace would change that
# silently. Profiles are workspace-global — the root's `[profile.release]` is
# the only one cargo reads — so a member cannot keep a release profile of its
# own, whatever the per-package override table (bottom of this file) can express.
exclude = [
  # Not in the app's dependency graph at all: a template binary that
  # `commands::knowledge_export` compiles on demand at runtime with
  # `--manifest-path`, carrying its own `[profile.release]` (`panic = "abort"`,
  # thin LTO). As a member it would rebuild under the app's fat-LTO/unwind
  # profile instead.
  "crates/atlas-kb-server",
]

# ─── Vendored engine (ADR-0003 fork point `42b5f05`; renamed in ADR-0011) ──────────
# The engine closure, vendored whole: no submodule, no upstream remote, no
# upstream tracking (ADR-0003, D2, D4 — issue #42). Copied from
# the upstream repository @ 42b5f05 with `target/` excluded; upstream's LICENSE and
# NOTICE travel with the code in `vendor/atlas-engine/`, as Apache-2.0 §4 requires.
#
# **"Quarantined" here means the phone-home PATHS are severed, not that the
# crates are unused** — the Sep-03 audit misread the old wording exactly that
# way, so say it precisely: 104 of these 110 members ARE in the shipping
# binary's closure. What does not ship is their telemetry: `atlas-engine-analytics`
# had its destination, POST and capture sink removed (#43, spec D2; the local
# fact-reducer stays for its 79 tests), `atlas-engine-otel` defaults to
# `exporter: None` (upstream defaulted to a hardcoded Statsig endpoint), and
# `atlas-engine-feedback`'s uploader is gone — its `sentry` dep is now `sentry-types`
# for one test-only attachment builder. `tests/engine-quarantine.test.ts`
# documents which crates the containment rules cover.
#
# Closure size, because it is not the number the spec quotes: the Port
# Inventory's "77 crates, 63 droppable" is the closure of `atlas-engine-core`. D1
# chose the in-process **app-server client**, whose closure is larger — it also
# pulls `atlas-engine-app-server` itself and the `ext/` extensions the inventory lists
# as droppable. 105 crates normal+build, 110 including the five test-support
# crates the vendored crates' own dev-dependencies need (the spec's Testing
# Decisions keep upstream's suite). Upstream has 140 members, so 30 are
# dropped, not 63.

[workspace.package]

version = "0.0.0"
# Track the edition for all workspace crates in one place. Individual
# crates can still override this value, but keeping it here means new
# crates created with `cargo new -w ...` automatically inherit the 2024
# edition.
edition = "2024"
license = "Apache-2.0"

# Two things this table does to Atlas's own build, both disclosed rather than
# absorbed (#42, "behaves identically"):
#
#   - `tar = "=0.4.45"` is an EXACT pin, and `atlas-agent-store` asks for `0.4`.
#     Cargo intersects them, so Atlas's `tar` moved 0.4.46 -> 0.4.45. A patch
#     step within one semver-compatible major; the alternative is editing the
#     fork's pin, which belongs to the rip-out phase, not to vendoring.
#   - Duplicate majors. The engine brings older majors of ~60 crates Atlas
#     already uses (http 0.2 beside 1.4, zbus 4 beside 5, windows 0.58 beside
#     0.61, ...). They coexist; Atlas's own version is retained in every case,
#     and none of them reaches the shipping app's build graph — verified with
#     `cargo tree -p atlas --edges normal`, not inferred from the lockfile,
#     because `cargo metadata`'s resolve graph is a workspace-wide union and
#     reports edges the app never compiles.

# Upstream's workspace dependency table, verbatim except that internal `path`
# entries are rebased onto `vendor/atlas-engine/`. 24 entries naming crates outside the
# vendored closure are dropped rather than left dangling.
[workspace.dependencies]

# Internal
app_test_support = { path = "vendor/atlas-engine/app-server/tests/common" }
atlas-engine-analytics = { path = "vendor/atlas-engine/analytics" }
atlas-engine-agent-graph-store = { path = "vendor/atlas-engine/agent-graph-store" }
atlas-engine-agent-identity = { path = "vendor/atlas-engine/agent-identity" }
atlas-engine-api = { path = "vendor/atlas-engine/atlas-engine-api" }
atlas-engine-aws-auth = { path = "vendor/atlas-engine/aws-auth" }
atlas-engine-app-server = { path = "vendor/atlas-engine/app-server" }
atlas-engine-app-server-transport = { path = "vendor/atlas-engine/app-server-transport" }
atlas-engine-app-server-client = { path = "vendor/atlas-engine/app-server-client" }
atlas-engine-app-server-protocol = { path = "vendor/atlas-engine/app-server-protocol" }
atlas-engine-app-server-protocol-noop-macros = { path = "vendor/atlas-engine/app-server-protocol-noop-macros" }
atlas-engine-apply-patch = { path = "vendor/atlas-engine/apply-patch" }
atlas-engine-arg0 = { path = "vendor/atlas-engine/arg0" }
atlas-engine-async-utils = { path = "vendor/atlas-engine/async-utils" }
atlas-engine-backend-client = { path = "vendor/atlas-engine/backend-client" }
atlas-engine-chatgpt = { path = "vendor/atlas-engine/chatgpt" }
atlas-engine-client = { path = "vendor/atlas-engine/atlas-engine-client" }
atlas-engine-collaboration-mode-templates = { path = "vendor/atlas-engine/collaboration-mode-templates" }
atlas-engine-cloud-config = { path = "vendor/atlas-engine/cloud-config" }
atlas-engine-code-mode = { path = "vendor/atlas-engine/code-mode" }
atlas-engine-code-mode-protocol = { path = "vendor/atlas-engine/code-mode-protocol" }
atlas-engine-home = { path = "vendor/atlas-engine/atlas-engine-home" }
atlas-engine-http-client = { path = "vendor/atlas-engine/http-client" }
atlas-engine-websocket-client = { path = "vendor/atlas-engine/websocket-client" }
atlas-engine-config = { path = "vendor/atlas-engine/config" }
atlas-engine-connectors = { path = "vendor/atlas-engine/connectors" }
atlas-engine-agent-extension = { path = "vendor/atlas-engine/ext/agent" }
atlas-engine-connectors-extension = { path = "vendor/atlas-engine/ext/connectors" }
atlas-engine-context-fragments = { path = "vendor/atlas-engine/context-fragments" }
atlas-engine-core = { path = "vendor/atlas-engine/core" }
atlas-engine-core-plugins = { path = "vendor/atlas-engine/core-plugins" }
atlas-engine-diagnostics = { path = "vendor/atlas-engine/diagnostics" }
atlas-engine-file-system = { path = "vendor/atlas-engine/file-system" }
atlas-engine-exec-server-protocol = { path = "vendor/atlas-engine/exec-server-protocol" }
atlas-engine-exec-server = { path = "vendor/atlas-engine/exec-server" }
atlas-engine-exec-server-test-support = { path = "vendor/atlas-engine/exec-server/tests/support" }
atlas-engine-execpolicy = { path = "vendor/atlas-engine/execpolicy" }
atlas-engine-extension-api = { path = "vendor/atlas-engine/ext/extension-api" }
atlas-engine-extension-items = { path = "vendor/atlas-engine/ext/items" }
atlas-engine-goal-extension = { path = "vendor/atlas-engine/ext/goal" }
atlas-engine-git-attribution = { path = "vendor/atlas-engine/ext/git-attribution" }
atlas-engine-guardian = { path = "vendor/atlas-engine/ext/guardian" }
atlas-engine-guardian-v2 = { path = "vendor/atlas-engine/ext/guardian-v2" }
atlas-engine-image-generation-extension = { path = "vendor/atlas-engine/ext/image-generation" }
atlas-engine-external-agent-migration = { path = "vendor/atlas-engine/external-agent-migration" }
atlas-engine-experimental-api-macros = { path = "vendor/atlas-engine/atlas-engine-experimental-api-macros" }
atlas-engine-features = { path = "vendor/atlas-engine/features" }
atlas-engine-feedback = { path = "vendor/atlas-engine/feedback" }
atlas-engine-install-context = { path = "vendor/atlas-engine/install-context" }
atlas-engine-file-search = { path = "vendor/atlas-engine/file-search" }
atlas-engine-file-watcher = { path = "vendor/atlas-engine/file-watcher" }
atlas-engine-git-utils = { path = "vendor/atlas-engine/git-utils" }
atlas-engine-hooks = { path = "vendor/atlas-engine/hooks" }
atlas-engine-history = { path = "vendor/atlas-engine/history" }
atlas-engine-keyring-store = { path = "vendor/atlas-engine/keyring-store" }
atlas-engine-linux-sandbox = { path = "vendor/atlas-engine/linux-sandbox" }
atlas-engine-login = { path = "vendor/atlas-engine/login" }
atlas-engine-memories-extension = { path = "vendor/atlas-engine/ext/memories" }
atlas-engine-web-search-extension = { path = "vendor/atlas-engine/ext/web-search" }
atlas-engine-memories-read = { path = "vendor/atlas-engine/memories/read" }
atlas-engine-memories-write = { path = "vendor/atlas-engine/memories/write" }
atlas-engine-mcp = { path = "vendor/atlas-engine/atlas-engine-mcp" }
atlas-engine-mcp-extension = { path = "vendor/atlas-engine/ext/mcp" }
atlas-engine-model-provider-info = { path = "vendor/atlas-engine/model-provider-info" }
atlas-engine-models-manager = { path = "vendor/atlas-engine/models-manager" }
atlas-engine-network-proxy = { path = "vendor/atlas-engine/network-proxy" }
atlas-engine-otel = { path = "vendor/atlas-engine/otel" }
atlas-engine-plugin = { path = "vendor/atlas-engine/plugin" }
atlas-engine-model-provider = { path = "vendor/atlas-engine/model-provider" }
atlas-engine-process-hardening = { path = "vendor/atlas-engine/process-hardening" }
atlas-engine-protocol = { path = "vendor/atlas-engine/protocol" }
atlas-engine-prompts = { path = "vendor/atlas-engine/prompts" }
atlas-engine-queue-extension = { path = "vendor/atlas-engine/ext/queue" }
atlas-engine-response-debug-context = { path = "vendor/atlas-engine/response-debug-context" }
atlas-engine-rmcp-client = { path = "vendor/atlas-engine/rmcp-client" }
atlas-engine-rollout = { path = "vendor/atlas-engine/rollout" }
atlas-engine-rollout-trace = { path = "vendor/atlas-engine/rollout-trace" }
atlas-engine-sandboxing = { path = "vendor/atlas-engine/sandboxing" }
atlas-engine-secrets = { path = "vendor/atlas-engine/secrets" }
atlas-engine-shell-command = { path = "vendor/atlas-engine/shell-command" }
atlas-engine-shell-escalation = { path = "vendor/atlas-engine/shell-escalation" }
atlas-engine-skills-extension = { path = "vendor/atlas-engine/ext/skills" }
atlas-engine-skills = { path = "vendor/atlas-engine/skills" }
atlas-engine-state = { path = "vendor/atlas-engine/state" }
atlas-engine-terminal-detection = { path = "vendor/atlas-engine/terminal-detection" }
atlas-engine-test-binary-support = { path = "vendor/atlas-engine/test-binary-support" }
atlas-engine-thread-store = { path = "vendor/atlas-engine/thread-store" }
atlas-engine-tools = { path = "vendor/atlas-engine/tools" }
atlas-engine-uds = { path = "vendor/atlas-engine/uds" }
atlas-engine-utils-absolute-path = { path = "vendor/atlas-engine/utils/absolute-path" }
atlas-engine-utils-audio = { path = "vendor/atlas-engine/utils/audio" }
atlas-engine-utils-cache = { path = "vendor/atlas-engine/utils/cache" }
atlas-engine-utils-cargo-bin = { path = "vendor/atlas-engine/utils/cargo-bin" }
atlas-engine-utils-cli = { path = "vendor/atlas-engine/utils/cli" }
atlas-engine-utils-home-dir = { path = "vendor/atlas-engine/utils/home-dir" }
atlas-engine-utils-image = { path = "vendor/atlas-engine/utils/image" }
atlas-engine-utils-json-to-toml = { path = "vendor/atlas-engine/utils/json-to-toml" }
atlas-engine-utils-output-truncation = { path = "vendor/atlas-engine/utils/output-truncation" }
atlas-engine-utils-path = { path = "vendor/atlas-engine/utils/path-utils" }
atlas-engine-utils-path-uri = { path = "vendor/atlas-engine/utils/path-uri" }
atlas-engine-utils-plugins = { path = "vendor/atlas-engine/utils/plugins" }
atlas-engine-utils-pty = { path = "vendor/atlas-engine/utils/pty" }
atlas-engine-utils-rustls-provider = { path = "vendor/atlas-engine/utils/rustls-provider" }
atlas-engine-utils-stream-parser = { path = "vendor/atlas-engine/utils/stream-parser" }
atlas-engine-utils-string = { path = "vendor/atlas-engine/utils/string" }
atlas-engine-utils-template = { path = "vendor/atlas-engine/utils/template" }
atlas-engine-workload-identity = { path = "vendor/atlas-engine/workload-identity" }
atlas-engine-windows-sandbox = { path = "vendor/atlas-engine/windows-sandbox-rs" }
core_test_support = { path = "vendor/atlas-engine/core/tests/common" }

# External
age = "0.11.1"
ansi-to-tui = "8.0.1"
anyhow = "1"
arboard = { version = "3", features = ["wayland-data-control"] }
arc-swap = "1.9.0"
assert_cmd = "2"
assert_matches = "1.5.0"
async-channel = "2.3.1"
async-io = "2.6.0"
async-stream = "0.3.6"
aws-config = "1"
aws-credential-types = "1"
aws-sigv4 = "1"
aws-types = "1"
axum = { version = "0.8", default-features = false }
base64 = "0.22.1"
bitflags = "2.13.1"
bm25 = "2.3.2"
bytes = "1.10.1"
chardetng = "0.1.17"
chrono = "0.4.43"
clap = "4"
clap_complete = "4"
clatter = { version = "2.2.0", default-features = false, features = [
    "alloc",
    "getrandom",
    "use-25519",
    "use-aes-gcm",
    "use-rust-crypto-ml-kem",
    "use-sha",
] }
color-eyre = "0.6.3"
constant_time_eq = "0.3.1"
crossbeam-channel = "0.5.15"
crypto_box = { version = "0.9.1", features = ["seal"] }
crossterm = "0.29.0"
ctor = "0.6.3"
deno_core_icudata = "0.77.0"
derive_more = "2"
diffy = "0.4.2"
dirs = "6"
divan = "0.1.21"
dns-lookup = "3.0.1"
dotenvy = "0.15.7"
dunce = "1.0.4"
ed25519-dalek = { version = "2.2.0", features = ["pkcs8"] }
encoding_rs = "0.8.35"
eventsource-stream = "0.2.3"
flate2 = "1.1.8"
futures = { version = "0.3", default-features = false }
gethostname = "1.1.0"
gix = { version = "0.83.0", default-features = false, features = ["sha1"] }
glob = "0.3"
globset = "0.4"
hmac = "0.12.1"
http = "1.3.1"
httpdate = "1.0.3"
iana-time-zone = "0.1.64"
icu_decimal = "2.1"
icu_locale_core = "2.1"
icu_provider = { version = "2.1", features = ["sync"] }
ignore = "0.4.23"
image = { version = "^0.25.9", default-features = false }
include_dir = "0.7.4"
indexmap = "2.12.0"
insta = "1.46.3"
inventory = "0.3.19"
itertools = "0.14.0"
jsonptr = { version = "0.7.1", default-features = false }
jsonwebtoken = { version = "10.4.0", default-features = false, features = [
    "use_pem",
    "aws_lc_rs",
] }
keyring = { version = "3.6", default-features = false }
landlock = "0.4.4"
lazy_static = "1"
libc = "0.2.182"
# Keep SQLx's bundled SQLite on a version containing the WAL-reset corruption fix:
# https://www.sqlite.org/wal.html#the_wal_reset_bug
libsqlite3-sys = { version = "0.37", default-features = false }
log = "0.4"
lru = "0.18.2"
maplit = "1.0.2"
memchr = "2.7.6"
mime_guess = "2.0.5"
multimap = "0.10.0"
notify = "8.2.0"
nucleo = { git = "https://github.com/helix-editor/nucleo.git", rev = "4253de9faabb4e5c6d81d946a5e35a90f87347ee" }
once_cell = "1.20.2"
openssl-sys = "*"
opentelemetry = "0.31.0"
opentelemetry-appender-tracing = "0.31.0"
opentelemetry-otlp = "0.31.0"
opentelemetry-semantic-conventions = "0.31.0"
opentelemetry_sdk = "0.31.0"
os_info = "3.12.0"
owo-colors = "4.3.0"
pathdiff = "0.2"
portable-pty = "0.9.0"
predicates = "3"
pretty_assertions = "1.4.1"
pulldown-cmark = { version = "0.10", default-features = false }
quick-xml = "0.41.0"
rand = "0.9"
ratatui = { version = "0.30.2", default-features = false, features = [
    "crossterm",
    "layout-cache",
    "underline-color",
] }
ratatui-macros = "0.7.2"
rcgen = { version = "0.14.7", default-features = false, features = [
    "aws_lc_rs",
    "pem",
] }
regex = "1.12.3"
regex-lite = "0.1.8"
# `default-features = false`: reqwest's default set is `default-tls` + the
# three below. `default-tls` is native-tls (Security.framework on macOS), which
# no consumer wants — every vendored and Atlas crate asks for `rustls-tls` —
# yet with both on, any `Client::new()` that never called `.use_rustls_tls()`
# silently took native-tls. Dropping it removes native-tls, hyper-tls and
# tokio-native-tls from the graph and puts every client on rustls/ring.
# `cookies` stays: atlas-engine-http-client implements `reqwest::cookie::CookieStore`.
reqwest = { version = "0.12", default-features = false, features = [
    "charset",
    "http2",
    "system-proxy",
    "cookies",
    "rustls-tls",
] }
# NOTE (Sep-03 audit): rmcp, rig-core and posthog-rs are the three crates
# holding a second reqwest (0.13) tower in the lock, duplicating hyper/h2/
# http/tower behind it. The collapse runs FORWARD, not back: all three have
# 0.13 as their floor in every current release, so unification means moving
# the app AND the vendored engine to 0.13 — an upstream-sync-sized change,
# not a pin. Left as-is deliberately.
rmcp = { version = "=3.0.0", default-features = false }
runfiles = { git = "https://github.com/dzbarsky/rules_rust", rev = "b56cbaa8465e74127f1ea216f813cd377295ad81" }
rustix = { version = "1.1.4", features = ["net"] }
rustls = { version = "0.23", default-features = false, features = [
    "aws_lc_rs",
    "std",
] }
rustls-native-certs = "0.8.3"
rustls-pki-types = "1.14.0"
schemars = "0.8.22"
seccompiler = "0.5.0"
semver = "1.0"
# Types only. The full `sentry` client (transport, panic hooks, its own HTTP
# stack) was pulled for ONE dead function's `protocol::Attachment` structs —
# feedback's uploader was removed with the rest of the phone-home paths (#43)
# and only its tests still build attachments.
sentry-types = "0.46.0"
serde = { version = "1", features = ["rc"] }
serde_ignored = "0.1.14"
serde_json = "1"
serde_path_to_error = "0.1.20"
serde_with = "3.17"
serde_yaml = "0.9"
serial_test = "3.2.0"
sha1 = "0.10.6"
scopeguard = "1.2.0"
sha2 = "0.10"
shlex = "1.3.0"
similar = "2.7.0"
symphonia = { version = "0.6.0", default-features = false, features = [
    "isomp4",
    "mkv",
    "mp3",
    "ogg",
    "wav",
] }
socket2 = "0.6.1"
# When bumping sqlx, audit the SQLite constructor deny list in clippy.toml.
sqlx = { version = "0.9.0", default-features = false, features = [
    "chrono",
    "json",
    "macros",
    "migrate",
    "runtime-tokio",
    "tls-rustls",
    "sqlite-bundled",
    "time",
    "uuid",
] }
starlark = { version = "0.14.2", default-features = false }
strum = "0.27.2"
strum_macros = "0.28.0"
supports-color = "3.0.2"
syntect = "5"
sys-locale = "0.3.2"
system-configuration = "0.7"
tar = { version = "=0.4.45", default-features = false }
tempfile = "3.23.0"
test-log = "0.2.19"
textwrap = "0.16.2"
thiserror = "2.0.17"
time = "0.3.47"
tiny_http = "0.12"
tokio = "1"
tokio-rustls = "0.26.4"
tokio-stream = "0.1.18"
tokio-test = "0.4"
tokio-tungstenite = { version = "0.28.0", features = [
    "proxy",
    "rustls-tls-native-roots",
] }
tokio-util = "0.7.18"
toml = "0.9.5"
toml_edit = "0.24.0"
tracing = "0.1.44"
tracing-appender = "0.2.3"
tracing-opentelemetry = "0.32.0"
tracing-subscriber = "0.3.22"
tracing-test = "0.2.5"
tonic = { version = "0.14.3", default-features = false, features = ["channel", "codegen"] }
tonic-prost = "0.14.3"
# BLOCKER B (#39/#42): `tree-sitter` sets `links = "tree-sitter"`, so the fork
# and `atlas-codeindex` cannot hold different majors. D4 resolves it by
# bumping the fork rather than pinning Atlas back: upstream was 0.25.10 here.
# `tree-sitter-bash` stays at 0.25 — there is no 0.26 of it, and it does not
# need one: grammar crates bind to the `tree-sitter-language` shim rather than
# to the core, which is exactly what lets one core serve grammars of several
# vintages (Atlas's own are 0.23).
tree-sitter = "0.26"
tree-sitter-bash = "0.25"
ts-rs = "11"
tungstenite = { version = "0.27.0", features = ["deflate", "proxy"] }
uds_windows = "1.1.0"
unicode-segmentation = "1.12.0"
unicode-width = "0.2"
url = "2"
urlencoding = "2.1"
uuid = "1"
v8 = "=150.4.0"
vt100 = "0.16.2"
walkdir = "2.5.0"
webbrowser = "1.2.2"
which = "8"
whoami = "1.6.1"
wildmatch = "2.6.1"
winapi-util = "0.1.11"
zip = "2.4.2"
zstd = "0.13"

wiremock = "0.6"
zeroize = "1.8.2"

[workspace.lints]

# `unexpected_cfgs` is a deny because its warning form already cost a live
# failure: #54 deleted the `ported-engine` feature, four blocks stayed behind
# `#[cfg(feature = "ported-engine")]`, and a cfg on a feature that no longer
# exists compiles to NOTHING — the token-source registration vanished and the
# first real gateway turn went out with no Authorization header. Cargo warned;
# nobody greps warnings. Now it fails the build.
rust = { unexpected_cfgs = "deny" }

[workspace.lints.clippy]

# `unwrap_used` and `expect_used` are STAGED, not enforced: turning the table
# on for the first-party members (#60) surfaced ~220 violations of the pair,
# overwhelmingly in test code where a panic IS the failure report. Re-add them
# behind a cleanup pass rather than allowlisting two hundred call sites in one
# sweep. Every other deny below is live and clean.
await_holding_invalid_type = "deny"
await_holding_lock = "deny"
disallowed_methods = "deny"
identity_op = "deny"
manual_clamp = "deny"
manual_filter = "deny"
manual_find = "deny"
manual_flatten = "deny"
manual_map = "deny"
manual_memcpy = "deny"
manual_non_exhaustive = "deny"
manual_ok_or = "deny"
manual_range_contains = "deny"
manual_retain = "deny"
manual_strip = "deny"
manual_try_fold = "deny"
manual_unwrap_or = "deny"
needless_borrow = "deny"
needless_borrowed_reference = "deny"
needless_collect = "deny"
needless_late_init = "deny"
needless_option_as_deref = "deny"
needless_question_mark = "deny"
needless_update = "deny"
redundant_clone = "deny"
redundant_closure = "deny"
redundant_closure_for_method_calls = "deny"
redundant_static_lifetimes = "deny"
trivially_copy_pass_by_ref = "deny"
uninlined_format_args = "deny"
unnecessary_filter_map = "deny"
unnecessary_lazy_evaluations = "deny"
unnecessary_sort_by = "deny"
unnecessary_to_owned = "deny"

# cargo-shear cannot see the platform-specific openssl-sys usage, so we
# silence the false positive here instead of deleting a real dependency.

# ─── Previous native SDK — gone (#54) ─────────────────────────────────────────────────
# The old SDK's two vendored-fork patch entries lived here. They
# carried two fixes the published crates lacked: incremental UTF-8 decoding in
# the SSE decoders, and racing `tool.execute()` against the cancel token.
#
# Both failures are still guarded, by tests against the engine that replaced
# them rather than by a patch table: a chunk-split fixture that splits an SSE
# frame at every byte position, and a cancel test that asserts on the
# filesystem — the killed command's marker file never appears.
[patch.crates-io]

# ─── The vendored engine's own git forks ─────────────────────────────────────
# Copied from the fork point's `[patch.crates-io]`. A patch table is honored
# only in the manifest cargo was invoked on, so upstream's copy is inert now
# that these crates are members here: without these three lines the engine
# silently resolves the unforked crates instead (#42).
# crossterm is deliberately NOT patched here, though upstream patches it.
# Its only consumer is the TUI, which is outside the vendored closure, so
# carrying the entry earned a "patch was not used in the crate graph"
# warning on every cargo invocation and patched nothing. Restore it with
# the TUI, if the TUI is ever ported.
tokio-tungstenite = { git = "https://github.com/openai-oss-forks/tokio-tungstenite", rev = "0e5b2d73aa18dd9f0a50ee9ff199d5aef7594186" }
tungstenite = { git = "https://github.com/openai-oss-forks/tungstenite-rs", rev = "4fffad30fe373adbdcffab9545e9e9bf4f2fc19f" }

# Upstream also patches the ssh spelling of the same fork, because a dependency
# names it that way. Verbatim; dropping it resolves tungstenite twice.
[patch."ssh://git@github.com/openai-oss-forks/tungstenite-rs.git"]
tungstenite = { git = "https://github.com/openai-oss-forks/tungstenite-rs", rev = "4fffad30fe373adbdcffab9545e9e9bf4f2fc19f" }

# Release profile. Thin LTO, not fat: measured 2026-09-04 on an M4 Pro at
# 422af792 (docs/research/build-performance.md) fat LTO's final link is a
# 12-minute single-threaded unit that reruns on every rebuild — one-file touch
# 14m59s, clean 23m05s — for a binary 19 MB smaller. Thin LTO at the same
# codegen-units, same tree: touch 4m16s, clean 8m14s, 170 MB vs 151 MB.
# Nothing upstream of that unit can shorten it; making the inputs cheaper to
# produce (codegen-units = 16 on the dependencies) made the fat merge *worse*,
# 38 minutes for the link alone. `codegen-units = 1` stays: raising it buys
# ~50 s per touch for +74 MB of binary (thin/cgu16 measured 243 MB).
# Before flipping this back to "fat", measure — the research doc has the method.
[profile.release]
codegen-units = 1   # Single codegen unit → better cross-function inlining
lto = "thin"        # Thin LTO: cross-crate inlining and DCE in parallel, per
                    # codegen unit, instead of one serial whole-program merge
strip = "symbols"   # Smaller .app bundle, faster dyld load
panic = "unwind"    # REQUIRED — three independent `catch_unwind` guards depend on
                    # it, so this survived the local-LLM removal:
                    #  1. atlas-embed's EMBEDDER catches candle's Metal
                    #     kernel-compile panic (at load AND per forward) to fall
                    #     back to CPU rather than crash.
                    #  2. commands/capture.rs guards its worker threads — a
                    #     panicking worker is silent capture loss.
                    #  3. atlas-checkpoint wraps atlas-redact, so a redaction
                    #     panic can never leak unredacted text.
                    # `abort` would turn each of these into an app crash. Costs a
                    # little binary size for unwind tables.
opt-level = 3       # Default for release, restated for clarity

# Build scripts and proc-macro crates are COMPILE-TIME ONLY — none of their
# code is linked into the shipped binary, and LTO never touches them. Without
# this stanza they inherit `opt-level = 3` / `codegen-units = 1` from the
# profile above, so the build spends real time optimizing 317 build scripts and
# 143 proc-macro dylibs whose only job is to run once and exit.
#
# `opt-level = 0` makes them compile fast and run slower; the trade is only
# worth it while the running is cheap relative to the compiling, which it is
# here (measure before assuming it holds — a proc-macro-bound build can invert
# it). Shipped output is bit-identical either way: this cannot affect binary
# size, speed, or the `panic = "unwind"` guarantees above.
[profile.release.build-override]
opt-level = 0
codegen-units = 256

# Dev profile — Atlas's own app crate is small (~170 LOC + a handful of
# command modules). Keep it at the default `opt-level = 0` so incremental
# rebuilds stay snappy. Its per-package stanza is deliberately absent below.
[profile.dev]
# Full DWARF for 130 members + their deps produced a 104 GB `target/debug` and
# paid for itself on every link. `line-tables-only` keeps what is actually read
# day to day — panic backtraces with file:line, and profiler symbols — and drops
# only variable inspection in a debugger. Restore `debug = true` locally (never
# commit it) on the rare occasion you need to step through Rust in lldb.
debug = "line-tables-only"

# Optimize ALL transitive dependencies in dev mode at `opt-level = 1`
# (Tauri, wry, tao, tokio, serde, reqwest, the old vendored SDK forks, etc.).
# First clean build pays a one-time ~3–5 minute cost; subsequent rebuilds only
# recompile the app crate and are unaffected. Net effect: `bunx tauri dev`
# startup drops noticeably because Tauri's runtime is no longer running
# unoptimized debug code.
[profile.dev.package."*"]
opt-level = 1
# No debug info for third-party dependencies. `[profile.dev]` keeps
# `line-tables-only` for workspace members, which is where file:line in a
# backtrace is ever read; for the ~1,000 crates below, DWARF was emitted on
# every clean build and sat in target/debug/deps for nothing. Function names
# and `panic!` locations do not come from debug info and are unaffected.
debug = false

# `"*"` above reaches dependencies ONLY — workspace members are excluded from
# it by cargo. Before the workspace, every `crates/atlas-*` package was a plain
# path dependency of src-tauri and therefore covered; as members they would
# silently drop to opt-level 0 and `tauri dev` would run the ACP stack, the
# terminal, the embedder and the RAG engine unoptimized. Restating the level
# per member is the only way to express "unchanged" (D4: "preserve dev-profile
# opt-levels for workspace members explicitly").
#
# Adding a member means adding a stanza here; `tests/cargo-workspace.test.ts`
# fails if one is missing.
[profile.dev.package.atlas-acp-thread]
opt-level = 1
[profile.dev.package.atlas-agent-delta]
opt-level = 1
[profile.dev.package.atlas-agent-manager]
opt-level = 1
[profile.dev.package.atlas-agent-servers]
opt-level = 1
[profile.dev.package.atlas-agent-store]
opt-level = 1
[profile.dev.package.atlas-agent-transcript]
opt-level = 1
[profile.dev.package.atlas-agent-wire]
opt-level = 1
[profile.dev.package.atlas-bus]
opt-level = 1
[profile.dev.package.atlas-checkpoint]
opt-level = 1
[profile.dev.package.atlas-codeindex]
opt-level = 1
[profile.dev.package.atlas-comms]
opt-level = 1
[profile.dev.package.atlas-embed]
opt-level = 1
[profile.dev.package.atlas-git]
opt-level = 1
[profile.dev.package.atlas-gitdiff]
opt-level = 1
[profile.dev.package.atlas-icon-theme]
opt-level = 1
[profile.dev.package.atlas-memory]
opt-level = 1
[profile.dev.package.atlas-native-agent]
opt-level = 1
[profile.dev.package.atlas-process]
opt-level = 1
[profile.dev.package.atlas-redact]
opt-level = 1
[profile.dev.package.atlas-terminal]
opt-level = 1
[profile.dev.package.atlas-theme]
opt-level = 1
[profile.dev.package.atlas-thread-metadata]
opt-level = 1

# The vendored engine's members, same reasoning as the stanzas above (#65).
# #42's exemption — "quarantined, on no runtime path until the seam is rewired
# (#45)" — expired when #45 and #54 landed: the engine now runs on EVERY dev
# turn (streaming, rollout I/O, sandboxing, apply-patch, ~600k LOC), and at
# the profile's opt-level 0 it was the slowest code on the hottest path. The
# cost is a longer first clean build; the `"*"` override cannot cover these
# because workspace members are excluded from it by cargo. Generated from the
# member list — a new vendored member belongs here too, and
# tests/cargo-workspace.test.ts fails if it is missing.
#
# `incremental = false` on every one of them: these crates change only when
# the fork is patched, so incremental state (14.6 GB of target/debug, the
# bulk of it) was write-once and never re-read. Non-incremental codegen is
# also faster on a first compile and, unlike incremental units, is cacheable
# by a rustc wrapper such as sccache. The trade: editing a vendored crate
# recompiles that crate from scratch. Atlas's own members stay incremental.

[profile.dev.package.app_test_support]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-agent-extension]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-agent-graph-store]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-agent-identity]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-analytics]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-api]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-app-server]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-app-server-client]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-app-server-protocol]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-app-server-protocol-noop-macros]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-app-server-transport]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-apply-patch]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-arg0]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-async-utils]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-aws-auth]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-backend-client]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-backend-openapi-models]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-chatgpt]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-client]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-cloud-config]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-code-mode]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-code-mode-protocol]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-collaboration-mode-templates]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-config]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-connectors]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-connectors-extension]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-context-fragments]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-core]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-core-plugins]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-diagnostics]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-exec-server]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-exec-server-protocol]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-exec-server-test-support]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-execpolicy]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-experimental-api-macros]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-extension-api]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-extension-items]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-external-agent-migration]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-features]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-feedback]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-file-search]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-file-system]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-file-watcher]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-git-attribution]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-git-utils]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-goal-extension]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-guardian]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-guardian-v2]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-history]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-home]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-hooks]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-http-client]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-image-generation-extension]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-install-context]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-keyring-store]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-linux-sandbox]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-login]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-mcp]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-mcp-extension]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-memories-extension]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-memories-read]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-memories-write]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-model-provider]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-model-provider-info]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-models-manager]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-network-proxy]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-otel]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-plugin]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-process-hardening]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-prompts]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-protocol]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-queue-extension]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-response-debug-context]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-rmcp-client]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-rollout]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-rollout-trace]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-sandboxing]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-secrets]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-shell-command]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-shell-escalation]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-skills]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-skills-extension]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-state]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-terminal-detection]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-test-binary-support]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-thread-store]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-tools]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-uds]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-absolute-path]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-audio]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-cache]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-cargo-bin]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-cli]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-home-dir]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-image]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-json-to-toml]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-output-truncation]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-path]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-path-uri]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-plugins]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-pty]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-rustls-provider]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-stream-parser]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-string]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-utils-template]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-web-search-extension]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-websocket-client]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-windows-sandbox]
opt-level = 1
incremental = false

[profile.dev.package.atlas-engine-workload-identity]
opt-level = 1
incremental = false

[profile.dev.package.core_test_support]
opt-level = 1
incremental = false
